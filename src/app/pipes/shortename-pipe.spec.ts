import { ShortenamePipe } from './shortename-pipe';

describe('ShortenamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenamePipe();
    expect(pipe).toBeTruthy();
  });
});
